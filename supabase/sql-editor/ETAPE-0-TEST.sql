-- ============================================================
-- ETAPE 0 — TEST (copiez UNIQUEMENT ce qui est entre les lignes)
-- Si vous voyez "TEST OK", le SQL Editor fonctionne.
-- ============================================================

SELECT 'TEST OK — le SQL Editor fonctionne' AS resultat, now() AS date_heure;
