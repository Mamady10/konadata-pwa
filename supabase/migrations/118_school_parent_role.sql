-- Les parents/tuteurs n'ont pas de rôle distinct : ils utilisent soit le compte
-- élève (email/mot de passe de l'enfant inscrit), soit le portail public
-- /suivi-scolarite (matricule + téléphone tuteur, OTP).
-- L'accès « Vie scolaire » côté établissement est géré pour les rôles student/candidate.

SELECT 1;
